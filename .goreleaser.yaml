# Generated by distui - https://github.com/WillyV3/vinw-workspace
# Edit as needed, distui won't overwrite manual changes

version: 2

before:
  hooks:
    - go test ./...
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    binary: vinw-workspace

archives:
  - format_overrides:
      - goos: windows
        format: zip
    files:
      - none*

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

brews:
  - name: vinw-workspace
    repository:
      owner: willyv3
      name: homebrew-tap
      token: "{{ .Env.GITHUB_TOKEN }}"
    homepage: https://github.com/WillyV3/vinw-workspace
    description: "vinw-workspace"
    license: MIT
    directory: Formula
    commit_author:
      name: distui
      email: distui@users.noreply.github.com
    commit_msg_template: "Brew formula update for vinw-workspace version {{ .Tag }}"
    test: |
      system "#{bin}/vinw-workspace", "--version"

# NPM publishing requires package.json in repo
# Run 'distui generate package.json' if not present

